# cms-clfs-downloader
Automated Python pipeline that downloads CMS's quarterly Clinical Laboratory Fee Schedule (CLFS) release, extracts and parses the data, generates a MySQL load script, and executes it to build a local HCPCS rates database
