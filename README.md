# cms-clfs-downloader
Python + SQL toolkit that downloads Medicare fee schedule data from CMS (HCPCS, CLFS, DMEPOS, and Anesthesiologist rates), parses it, and loads it into a local MySQL database — includes both a manual SQL loader and automated Python scrapers for each dataset.
